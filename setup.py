#!/usr/bin/env python

from setuptools import setup

setup(name='nicendredi',
      version='1.0',
      # list folders, not files
      packages=['nicendredi',
                'nicendredi.dessin'],
      )
